.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Recherche-Rohdaten (große Git-Repos, gehören nicht ins Hauptrepo)
/research/
/research-v2/

# Hinweis: data/external/ und static/questions/ werden bewusst eingecheckt,
# damit das Repo nach git clone direkt lauffähig ist (auch im GH-Build).
# Neu generieren via: node scripts/seed-questions.mjs
/.env

# WWM-Original-Sounds (Urheberrecht RTL/Endemol — nicht ins Repo).
# User lädt selbst via: node scripts/download-sounds.mjs
/static/sounds/
